{
  "name": "@solow/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./schema": "./src/schema.ts",
    "./testing": "./src/testing.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "lint": "biome check .",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "bun run src/migrate.ts",
    "db:bootstrap": "bun run src/bootstrap.ts"
  },
  "dependencies": {
    "@solow/contracts": "workspace:*",
    "@solow/core": "workspace:*",
    "drizzle-orm": "^0.45.2",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "bun-types": "^1.1.0",
    "drizzle-kit": "^0.31",
    "typescript": "^5.6.0"
  }
}
