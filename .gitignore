# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
build/
.next/
# The E2E suite builds into its own directory so it cannot overwrite the one a running
# `scripts/start.sh` stack is serving (see apps/web/next.config.mjs).
.next-e2e/
next-env.d.ts
out/
*.tsbuildinfo

# Agent scratch worktrees — never part of the project
.claude/worktrees/

# Local data (SoloW keeps SQLite DB + worktrees under ~/.solow by default)
*.sqlite
*.sqlite-*
/.solow/
/data/

# Env & secrets
.env
.env.*
!.env.example

# Logs & temp
*.log
coverage/
.turbo/
*.tmp
*.swp

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Test artifacts
playwright-report/
test-results/

# Local-only agent + dev-server state (never part of the project)
.claude/settings.local.json
.inngest/
